{
  "name": "azure-devops-extension-bundler-shelljs-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf out/*.js",
    "build": "esbuild task.js --platform=node --external:shelljs --bundle --outfile=out/task.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "4.12.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}
